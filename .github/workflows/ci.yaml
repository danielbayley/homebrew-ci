name: Homebrew CI
on:
  push: &paths
    paths:
    - .github/workflows/*.y*ml
    - Casks/*.rb
    - Formula/*.rb
    - cmd/**.rb

  pull_request: *paths

jobs:
  CI:
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        include:
        - runner: macos-14 # Sonoma
          name: macOS 14 Sonoma (ARM)
        - runner: macos-15 # Sequoia
          name: macOS 15 Sequoia (ARM)
        - runner: macos-15-intel
          name: macOS 15 Sequoia (x86)
        - runner: macos-26 # Tahoe
          name: macOS 26 Tahoe (ARM)
        - runner: ubuntu-22.04 #-latest
          name: Linux Ubuntu 22
        - runner: ubuntu-24.04
          name: Linux Ubuntu 24

    env:
      ACTIONS_STEP_DEBUG: ${{ vars.ACTIONS_STEP_DEBUG }}
      HOMEBREW_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    steps:
    - name: Set up Homebrew
      id: set-up-homebrew
      uses: Homebrew/actions/setup-homebrew@main

    - name: Install Homebrew Bundler RubyGems
      if: steps.cache.outputs.cache-hit != 'true'
      run: brew install-bundler-gems

    - run: echo ${GITHUB_REPOSITORY/homebrew-/}

    - name: Tap ${{ github.repository_owner }}/${{ github.event.repository.name }}
      run: brew tap ${GITHUB_REPOSITORY/homebrew-/}



    
